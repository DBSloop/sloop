# The Angular 21 toolchain needs Node 20.19 / 22.12 / 24 or newer, and the
# failure without it is a confusing stack trace rather than a sentence. `engines`
# in package.json only warns on its own; this turns it into a refusal to install.
engine-strict=true
